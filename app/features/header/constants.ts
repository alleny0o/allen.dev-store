/**
 * Breakpoint where header switches from mobile to desktop layout.
 * - Below this: Mobile layout (hamburger menu, simplified actions)
 * - At/above this: Desktop layout (horizontal nav, mega menus)
 */
export const HEADER_BREAKPOINT = 'lg';
export const HEADER_BREAKPOINT_PX = 1024;